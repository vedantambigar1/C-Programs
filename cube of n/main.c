#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n , cube;
    printf("Enter the number: ");
    scanf("%d", &n);

    cube = n * n * n;

    printf("cube is : %d", cube);
    return 0;
}
